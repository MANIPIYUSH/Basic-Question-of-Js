//8. Use search to find the position of the first occurrence of the
// word because in the following sentence:'You cannot end a sentence
// with because because because is a conjunction'.

let findPosition = "You cannot end a sentence with because because because is a conjunction";
let result = findPosition.search("because");
console.log(result);