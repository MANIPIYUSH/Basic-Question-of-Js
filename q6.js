//6. 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon' split the string at the comma and change it to an array.

let company = 'facebook google microsoft apple ibm oracle amazon';

let result = company.split(" ");
console.log(result);